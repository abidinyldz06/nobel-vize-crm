# Trusted project defaults; explicit session/CLI overrides may take precedence.
# Inherit existing MCP/plugin choices; do not duplicate integrations here.
model = "gpt-5.6"
web_search = "cached"
approval_policy = "on-request"
approvals_reviewer = "user"
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
network_access = false
